import numpy as np
import matplotlib.pyplot as plt

# 資料
x = np.array([0, 1, 2, 3, 4], dtype=np.float32)
y = np.array([1.9, 3.1, 3.9, 5.0, 6.2], dtype=np.float32)

# y = mx + b
def predict(m, b, x):
    return m * x + b

# MSE
def MSE(m, b):
    return np.mean((predict(m, b, x) - y) ** 2)
